[project]
name = "aggression-detection"
version = "0.1.0"
description = "YOLO11s + LightGBM + LSTM pipeline for aggression detection in surveillance video."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

